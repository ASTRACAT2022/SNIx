version: '3.8'

services:
  sni-proxy:
    build: .
    container_name: sni_proxy
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./config.json:/root/config.json:ro
      - ./logs:/root/logs
    cap_add:
      - NET_BIND_SERVICE
